// import { generateTest } from "./generate-test/index";
// import { Question, Choice } from "@/types";

// self.onmessage = async ({ data }) => {
//   const { topic } = data;

//   await generateTest(topic);

//   self.postMessage("done");
// };
